# Plan: Convert Frontend API Calls to Axios

## Current State
- All API calls use a custom `apiClient` based on browser's native `fetch` API
- Located in `frontend/src/shared/lib/api/client.ts`
- 10 files contain API calls (API files + hooks that consume them)

## Files to Modify

### 1. `frontend/src/shared/lib/api/client.ts`
Replace `fetch`-based implementation with axios:
- Install axios first
- Create axios instance with base URL `/api`
- Configure interceptors for auth token and error handling
- Keep same `ApiException` class for error handling

### 2. `frontend/src/features/Auth/api/login.ts`
Update to use new axios-based client (minimal changes - same signature)

### 3. `frontend/src/features/Auth/api/register.ts`
Update to use new axios-based client (minimal changes - same signature)

### 4. `frontend/src/features/Transactions/api/index.ts`
Update all CRUD methods to use new axios-based client

### 5. `frontend/src/features/Categories/api/index.ts`
Update all CRUD methods to use new axios-based client

## Implementation Steps

1. **Install axios**: `cd frontend && npm install axios`
2. **Rewrite api/client.ts**: Replace fetch with axios instance
3. **Update Auth API files**: login.ts, register.ts
4. **Update Transactions API**: api/index.ts
5. **Update Categories API**: api/index.ts

## Axios Client Design

```typescript
// frontend/src/shared/lib/api/client.ts
import axios from 'axios';

const apiClient = axios.create({
  baseURL: '/api',
  headers: { 'Content-Type': 'application/json' }
});

apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    // Handle ApiException
    throw error.response?.data || error;
  }
);
```

## Note
React Query hooks (`features/*/hooks/index.ts`, `features/*/model/*.ts`) do NOT need changes - they only consume the API functions which maintain the same interface.
